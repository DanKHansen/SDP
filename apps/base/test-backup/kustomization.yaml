apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Only the ArgoCD Application — consumed by parent overlay (dev environment).
# The actual PostgreSQL manifests live in manifests/ subdirectory (synced by ArgoCD).
resources:
  - application.yaml